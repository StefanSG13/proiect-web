type ShoppingCartItem = {
    id: number;
    image: string;
    title: string;
    price: string;
}

const ShoppingCartPage = (product: ShoppingCartItem) => {
    return (
        <div>

        </div>
    );
};

export default ShoppingCartPage;